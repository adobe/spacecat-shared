# Intermediate conversion artifact — regenerated by `npm run spec:convert`, never committed.
build/

# Counterfact scratch dir (mock handlers materialized at runtime).
.counterfact/

# The generated Python package is committed (model.py, aiseo.py, http_server.py,
# __init__.py); only the bytecode caches are ignored.
python/**/__pycache__/
