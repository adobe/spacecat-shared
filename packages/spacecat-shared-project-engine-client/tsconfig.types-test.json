{
  "//": "Compile-only check of the public type surface AND the mock factories/fixtures. Not a build — `noEmit`. Run by `npm run test:types`; see test/types/*.ts. allowJs lets tsc read the `// @ts-check` mock files (factories.js, seeds.js) so fixture drift from the overlayed schema is a compile error.",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "esnext",
    "lib": ["esnext", "dom"],
    "strict": true,
    "noEmit": true,
    "allowJs": true,
    "checkJs": false,
    "skipLibCheck": true,
    "types": []
  },
  "include": [
    "test/types/**/*.ts",
    "src/index.d.ts",
    "src/generated/types.ts",
    "mock/factories.js",
    "mock/seeds.js"
  ]
}
