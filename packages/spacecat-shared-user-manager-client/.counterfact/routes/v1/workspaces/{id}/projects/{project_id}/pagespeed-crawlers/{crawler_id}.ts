import type { pagespeedUpdate } from "../../../../../../../types/paths/v1/workspaces/{id}/projects/{project_id}/pagespeed-crawlers/{crawler_id}.types.js";

export const PATCH: pagespeedUpdate = async ($) => {
  return $.response[200].random();
};
