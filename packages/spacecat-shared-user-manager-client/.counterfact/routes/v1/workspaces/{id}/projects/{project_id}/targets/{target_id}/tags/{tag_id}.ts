import type { projectUpdateTag } from "../../../../../../../../../types/paths/v1/workspaces/{id}/projects/{project_id}/targets/{target_id}/tags/{tag_id}.types.js";

export const PUT: projectUpdateTag = async ($) => {
  return $.response[200].random();
};
